#version 120

uniform sampler2D texture;
uniform float brightness;

void main()
{
    //float partial = sqrt(sqrt(brightness));
    gl_FragColor = texture2D(texture, gl_TexCoord[0].st) / max(min(4.5 * brightness, 1000.0), 1.0);
}