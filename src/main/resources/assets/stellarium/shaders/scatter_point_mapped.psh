#version 120

uniform vec4 invres2; // 0.5 * (pointSize / resolution)^2

varying vec4 color;

const vec2 center = vec2(0.5, 0.5);

void main()
{
    vec2 coord = gl_PointCoord - center;
    gl_FragColor = color * exp(- dot(coord, coord) * invres2);
}